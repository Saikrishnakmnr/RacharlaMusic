# RacharlaMusic
GitHub/Streamlit-ready Telugu + English AI song generator.

This version uses Hugging Face's public Gradio Space API via `gradio_client` rather than assuming the Space exposes the local REST server directly.

No API key is required for the public Space. Free ZeroGPU availability and quotas can vary.

Deploy with `streamlit_app.py`.
